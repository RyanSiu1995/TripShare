<%= form_for(@trip) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :title, placeholder: "Name a new trip..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
